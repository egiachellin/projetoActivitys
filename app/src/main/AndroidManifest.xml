<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjetoActivitys"
        tools:targetApi="31">
        <activity android:name=".sobre"></activity>
        <activity android:name=".login"></activity>
        <activity android:name=".loginFailed"></activity>
        <activity android:name=".loginSucesso"></activity>
        <activity android:name=".cadastro"></activity>
        <activity android:name=".cadastroFailed"></activity>
        <activity android:name=".cadastroSucesso"></activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>